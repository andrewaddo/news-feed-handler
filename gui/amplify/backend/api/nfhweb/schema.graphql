type ProfileConfig @model @key(fields: ["profile"]) {
  id: ID! #required
  profile: ID!
  webhookURL: String!
  searchConfigs: [SearchConfig] @connection(keyField: ["profile"])
}

type SearchConfig @model @key(keyField: ["profileID", "searchItem"]) {
  id: ID!
  profileID: ID!
  searchItem: String!
  searchString: String!
  rss: String!
  profileConfig: ProfileConfig @connection(keyField: ["profileID"])
  News: [News] @connection(name: "bySearchConfig", fields: ["id"])
}

type News @model @key(name: "bySearchConfig", fields: ["searchConfigID", "title"]) {
  id: ID!
  searchConfigID: ID!
  searchConfig: SearchConfig @connection(keyField: ["searchConfigID"])
  title: String!
}
