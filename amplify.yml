version: 0.1
backend:
  phases:
    preBuild:
      commands:
        - cd gui 
    build:
      commands:
        - envCache --set stackInfo ""
        - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: gui/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*